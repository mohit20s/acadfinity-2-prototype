"use client";

import { useState } from 'react';
import { ArrowLeft, ArrowRight, CheckCircle2, ClipboardCheck, BarChart, Lightbulb, Users, GraduationCap, Building, Lock, PlayCircle, Star } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { cn } from '@/lib/utils';
import { usePrototypeStore } from '@/store/use-prototype-store';

// Diagnostic Plans
const diagnosticPlans = [
  { id: 'Educational Institute', title: "Institute Health Check", target: "For Directors & Principals", price: "₹4,999", desc: "A complete AI audit of Educational Institute operations, academics, and finances.", icon: Building, color: "blue" },
  { id: 'teacher', title: "Educator Effectiveness", target: "For Teachers", price: "₹999", desc: "Analyze teaching methodologies and get personalized upskilling paths.", icon: Users, color: "emerald" },
  { id: 'student', title: "Student Learning Gap", target: "For Students & Parents", price: "₹499", desc: "Identify academic weak points and get curated course recommendations.", icon: GraduationCap, color: "amber" },
];

export default function DiagnosticToolPage() {
  const [step, setStep] = useState(0); // 0: Select Plan, 1: Pay/Start, 2-4: Questions, 5: Results
  const [selectedPlan, setSelectedPlan] = useState<string | null>(null);
  const { currentRole } = usePrototypeStore();

  const totalSteps = 3;
  // Progress calculation only starts after step 1
  const progress = step > 1 ? ((step - 1) / totalSteps) * 100 : 0;

  // --- STEP 0: SELECT DIAGNOSTIC PLAN (THE STOREFRONT) ---
  if (step === 0) {
    return (
      <div className="space-y-10 animate-in fade-in duration-500 pb-24">
        
        {/* Premium Banner */}
        <div className="bg-slate-900 text-white rounded-3xl p-6 md:p-10 flex flex-col md:flex-row justify-between items-center gap-8 relative overflow-hidden shadow-2xl">
          <div className="absolute -right-20 -top-20 w-96 h-96 bg-primary/20 rounded-full blur-3xl"></div>
          
          <div className="relative z-10 w-full text-center md:text-left">
            <div className="inline-flex items-center gap-2 rounded-full bg-emerald-500/20 border border-emerald-500/30 px-3 py-1 text-[10px] font-black mb-4 uppercase tracking-widest text-emerald-400">
              <Star className="h-3 w-3 fill-emerald-400" /> Premium Feature
            </div>
            <h1 className="text-3xl md:text-5xl font-black mb-4 tracking-tight leading-tight">AI-Powered<br/>Diagnostics Hub</h1>
            <p className="text-slate-400 max-w-xl text-sm md:text-base font-medium">
              Data-driven insights for every stakeholder. Select an assessment below to get a personalized, actionable growth report generated by our advanced AI engine.
            </p>
          </div>
          
          <div className="relative z-10 shrink-0">
             <div className="h-32 w-32 rounded-full border-8 border-white/10 flex items-center justify-center bg-primary/20 backdrop-blur-md shadow-2xl">
               <ClipboardCheck className="h-12 w-12 text-primary" />
             </div>
          </div>
        </div>

        {/* Pricing/Selection Grid */}
        <section className="space-y-6">
          <div className="text-center md:text-left">
             <h2 className="text-2xl font-black tracking-tight text-slate-900">Select Your Assessment</h2>
             <p className="text-sm font-medium text-slate-500">Choose the diagnostic report tailored to your specific role.</p>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {diagnosticPlans.map((plan) => {
               // Determine styling based on color
               const isBlue = plan.color === 'blue';
               const isEmerald = plan.color === 'emerald';
               const isAmber = plan.color === 'amber';
               
               return (
                <div key={plan.id} className="bg-white rounded-3xl border-2 border-slate-100 shadow-sm p-6 flex flex-col hover:border-primary/30 hover:shadow-xl transition-all group relative overflow-hidden">
                  
                  <div className="flex justify-between items-start mb-6">
                    <div className={cn("h-16 w-16 rounded-2xl flex items-center justify-center transition-transform group-hover:scale-110", 
                      isBlue ? "bg-blue-50 text-blue-600" : isEmerald ? "bg-emerald-50 text-emerald-600" : "bg-amber-50 text-amber-600"
                    )}>
                      <plan.icon className="h-8 w-8" />
                    </div>
                    <span className="bg-slate-900 text-white px-4 py-1.5 rounded-full text-lg font-black shadow-md">{plan.price}</span>
                  </div>
                  
                  <p className="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em] mb-1">{plan.target}</p>
                  <h3 className="font-black text-slate-900 text-xl leading-tight mb-3">{plan.title}</h3>
                  <p className="text-sm font-medium text-slate-500 mb-8">{plan.desc}</p>
                  
                  <div className="mt-auto">
                    <Button 
                      onClick={() => { setSelectedPlan(plan.id); setStep(1); }} 
                      className={cn("w-full h-12 rounded-xl font-black shadow-md text-sm", 
                        isBlue ? "bg-blue-600 hover:bg-blue-700" : isEmerald ? "bg-emerald-600 hover:bg-emerald-700" : "bg-amber-500 hover:bg-amber-600 text-slate-900"
                      )}
                    >
                      Purchase & Start <ArrowRight className="ml-2 h-4 w-4" />
                    </Button>
                  </div>
                </div>
               );
            })}
          </div>
        </section>

      </div>
    );
  }

  // --- STEP 1: PAYMENT CONFIRMATION (SIMULATED) ---
  if (step === 1) {
     const plan = diagnosticPlans.find(p => p.id === selectedPlan);
     return (
       <div className="flex items-center justify-center min-h-[60vh] animate-in zoom-in-95 duration-300">
         <div className="bg-white p-8 rounded-3xl border-2 border-primary shadow-2xl max-w-md w-full text-center">
            <div className="h-20 w-20 rounded-full bg-emerald-100 text-emerald-600 mx-auto flex items-center justify-center mb-6">
              <CheckCircle2 className="h-10 w-10" />
            </div>
            <h2 className="text-2xl font-black text-slate-900 mb-2">Payment Successful!</h2>
            <p className="text-sm font-medium text-slate-500 mb-6">You have unlocked the <strong className="text-slate-900">{plan?.title}</strong>. Your AI agent is ready to begin.</p>
            <Button onClick={() => setStep(2)} className="w-full h-14 rounded-xl font-black text-lg shadow-lg shadow-primary/20">
              Begin Assessment Now
            </Button>
         </div>
       </div>
     );
  }

  // --- FINAL STEP: RESULTS SCREEN ---
  if (step > totalSteps + 1) {
    const plan = diagnosticPlans.find(p => p.id === selectedPlan);
    return (
      <div className="space-y-6 animate-in fade-in duration-500 pb-20">
        <div className="text-center p-8 bg-slate-900 text-white rounded-3xl shadow-2xl relative overflow-hidden">
           <div className="absolute top-0 right-0 w-64 h-64 bg-emerald-500/20 rounded-full blur-3xl"></div>
           <BarChart className="h-12 w-12 text-emerald-400 mx-auto mb-4 relative z-10" />
           <h1 className="text-3xl md:text-4xl font-black mb-2 relative z-10">{plan?.title} Report</h1>
           <p className="text-slate-400 font-medium max-w-xl mx-auto relative z-10">Your customized AI health score and action plan is ready.</p>
        </div>
        
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div className="bg-white p-6 rounded-3xl border-2 border-slate-100 text-center shadow-sm hover:border-primary/20 transition-colors"><p className="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Overall Score</p><p className="text-5xl font-black text-primary mt-2">82<span className="text-2xl text-slate-300">/100</span></p></div>
          <div className="bg-white p-6 rounded-3xl border-2 border-slate-100 text-center shadow-sm hover:border-emerald-500/20 transition-colors"><p className="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Core Strength</p><p className="text-5xl font-black text-emerald-500 mt-2">A-</p></div>
          <div className="bg-white p-6 rounded-3xl border-2 border-slate-100 text-center shadow-sm hover:border-amber-500/20 transition-colors"><p className="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Improvement Area</p><p className="text-5xl font-black text-amber-500 mt-2">B+</p></div>
        </div>

        <div className="bg-white p-8 rounded-3xl border-2 border-slate-100 shadow-sm">
          <h3 className="font-black text-slate-900 flex items-center gap-2 mb-6 text-xl"><Lightbulb className="h-6 w-6 text-amber-500 fill-amber-500" /> AI Recommendations</h3>
          <div className="space-y-4">
            <div className="p-5 bg-blue-50 border border-blue-100 rounded-2xl flex gap-4 items-start">
               <div className="h-8 w-8 rounded-full bg-blue-200 text-blue-700 flex items-center justify-center font-black shrink-0">1</div>
               <p className="text-sm font-semibold text-blue-900 mt-1">Based on your answers, digitizing fee collection via the Acadfinity ERP will reduce manual effort by an estimated 30%.</p>
            </div>
            <div className="p-5 bg-emerald-50 border border-emerald-100 rounded-2xl flex gap-4 items-start">
               <div className="h-8 w-8 rounded-full bg-emerald-200 text-emerald-700 flex items-center justify-center font-black shrink-0">2</div>
               <p className="text-sm font-semibold text-emerald-900 mt-1">Introduce 2 new outskill courses from our Marketplace to boost engagement and create a new revenue stream.</p>
            </div>
          </div>
        </div>
        
        <div className="flex flex-col sm:flex-row justify-center gap-4 pt-4">
          <Button variant="outline" className="h-12 rounded-full font-black border-2" onClick={() => setStep(0)}>Take Another Assessment</Button>
          <Button className="h-12 rounded-full font-black px-8 shadow-lg">Download Full PDF Report</Button>
        </div>
      </div>
    )
  }

  // --- THE QUESTIONNAIRE WIZARD (Steps 2-4) ---
  return (
    <div className="max-w-2xl mx-auto space-y-8 p-4 md:p-0 animate-in fade-in duration-300">
      
      {/* Progress Bar */}
      <div className="space-y-3 pt-4">
        <div className="flex justify-between text-[10px] font-black text-slate-400 uppercase tracking-widest">
          <span>Step {step - 1} of {totalSteps}</span>
          <span className="text-primary">{Math.round(progress)}% Complete</span>
        </div>
        <div className="h-3 w-full bg-slate-100 rounded-full overflow-hidden">
          <div className="h-full bg-primary transition-all duration-500 ease-out" style={{width: `${progress}%`}}></div>
        </div>
      </div>

      {/* Question Card */}
      <div className="bg-white p-8 md:p-12 rounded-[2rem] border-2 border-slate-100 shadow-xl shadow-slate-200/50">
        {step === 2 && <div className="space-y-6">
          <div className="flex items-center gap-3 mb-8"><div className="h-12 w-12 rounded-2xl bg-blue-50 text-blue-600 flex items-center justify-center"><Building className="h-6 w-6" /></div><h2 className="text-2xl font-black text-slate-900">Demographics</h2></div>
          <div><label className="text-sm font-black text-slate-700 mb-2 block">Number of Students / Learner Age</label><select className="w-full h-14 px-4 border-2 border-slate-200 rounded-xl font-medium text-slate-600 outline-none focus:border-primary focus:ring-4 focus:ring-primary/10 transition-all"><option>Select an option...</option><option>500-1000</option><option>High Educational Institute</option></select></div>
        </div>}
        
        {step === 3 && <div className="space-y-6">
          <div className="flex items-center gap-3 mb-8"><div className="h-12 w-12 rounded-2xl bg-emerald-50 text-emerald-600 flex items-center justify-center"><GraduationCap className="h-6 w-6" /></div><h2 className="text-2xl font-black text-slate-900">Academics</h2></div>
          <div><label className="text-sm font-black text-slate-700 mb-2 block">What is your biggest learning challenge?</label><input type="text" placeholder="e.g., Engagement, Math scores..." className="w-full h-14 px-4 border-2 border-slate-200 rounded-xl font-medium text-slate-900 outline-none focus:border-primary focus:ring-4 focus:ring-primary/10 transition-all" /></div>
        </div>}
        
        {step === 4 && <div className="space-y-6">
          <div className="flex items-center gap-3 mb-8"><div className="h-12 w-12 rounded-2xl bg-amber-50 text-amber-600 flex items-center justify-center"><Users className="h-6 w-6" /></div><h2 className="text-2xl font-black text-slate-900">Operations</h2></div>
          <div><label className="text-sm font-black text-slate-700 mb-2 block">How do you currently track progress?</label><select className="w-full h-14 px-4 border-2 border-slate-200 rounded-xl font-medium text-slate-600 outline-none focus:border-primary focus:ring-4 focus:ring-primary/10 transition-all"><option>Manual Spreadsheets</option><option>Basic Software</option></select></div>
        </div>}
      </div>

      {/* Navigation Buttons */}
      <div className="flex justify-between pt-4">
        <Button variant="ghost" onClick={() => setStep(step - 1)} className="font-black text-slate-400 hover:text-slate-900 h-12 px-6 rounded-full">
          <ArrowLeft className="h-4 w-4 mr-2" /> Back
        </Button>
        <Button onClick={() => setStep(step + 1)} className="h-12 px-8 rounded-full font-black shadow-lg shadow-primary/20">
          {step === totalSteps + 1 ? 'Generate AI Report' : 'Next Step'}
        </Button>
      </div>
    </div>
  );
}